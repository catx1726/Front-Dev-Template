export default class User {
  public name: string
  public age: number
  public id: string
  constructor(name: string, age: number, id: string) {
    this.name = name
    this.age = age
    this.id = id
  }
}
