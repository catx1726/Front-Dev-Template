import { DomSpyDirective } from './dom-spy.directive';

describe('DomSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new DomSpyDirective();
    expect(directive).toBeTruthy();
  });
});
