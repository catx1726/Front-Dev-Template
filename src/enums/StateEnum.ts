export enum CheckStateEnum {
  OBTAINING_EVIDENCE,
  RATING,
  FINISHED
}

export enum CheckResultEnum{
  NONE,
  QUALIFIED,
  UNQUALIFIED
}

export enum TaskStateEnum{
  CREATE,
  TODO,
  SAVE,
  SUBMIT,
}